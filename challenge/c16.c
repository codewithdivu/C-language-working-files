#include<stdio.h>
#include<string.h>

int main(int argc, char const *argv[])
{
    char word[50];
    printf("enter the word \n");
    gets(word);

    for (int i = 0; i <= strlen(word); i++)
    {
        
    }
    
    return 0;
}
